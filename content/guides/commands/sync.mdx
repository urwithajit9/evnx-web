---
slug: "commands-sync"
section: "commands"
order: 2
title: "evnx sync"
publishedAt: "2024-03-15"
updatedAt: "2024-03-20"
excerpt: "Reference documentation for the evnx sync command: options, exit codes, and basic usage."
difficulty: "beginner"
timeToComplete: "5 minutes"
tags: ["sync", "reference", "cli", "env"]
evnxVersion: "0.2.1"
prerequisites:
  - getting-started/installation
draft: false
---

# evnx sync — Command Reference

Keep `.env` and `.env.example` in perfect synchronization with bidirectional sync, smart placeholders, and security guards.

<Prerequisites>
  <Prerequisite href="/guides/getting-started/installation">Install evnx</Prerequisite>
</Prerequisites>

---

## Command signature

```bash
evnx sync [OPTIONS]
```

### Options reference

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--direction`, `-d` | `enum` | `forward` | Sync direction: `forward` or `reverse` |
| `--placeholder`, `-p` | `bool` | `false` | Force placeholder values when adding variables |
| `--dry-run`, `-n` | `bool` | `false` | Preview changes without writing files |
| `--force`, `-f` | `bool` | `false` | Skip interactive prompts (CI/CD mode) |
| `--template-config` | `string` | — | Path to custom placeholder JSON config |
| `--naming-policy` | `enum` | `warn` | Handle non-standard names: `warn`, `error`, `ignore` |
| `--verbose`, `-v` | `bool` | `false` | Enable detailed debug output |
| `--help`, `-h` | `bool` | `false` | Display help information |

### Direction values

| Value | Flow | Use case |
|-------|------|----------|
| `forward` | `.env` → `.env.example` | Update team template with your new variables |
| `reverse` | `.env.example` → `.env` | Pull team changes to your local `.env` |

### Naming policy values

| Policy | Behavior | Best for |
|--------|----------|----------|
| `warn` | Shows warning, continues | Local development (default) |
| `error` | Fails on non-standard names | CI/CD validation |
| `ignore` | No validation | Legacy projects |

---

## Exit codes

| Code | Meaning | Use case |
|------|---------|----------|
| `0` | ✅ Sync completed successfully | Success in scripts/CI |
| `1` | 🚨 Sync failed (missing files, validation errors) | Fail pipeline on issues |
| `2+` | ⚠️ Runtime errors (IO, parsing failures) | Debug tooling issues |

---

## Basic usage examples

### Forward sync (default)
```bash
# Update .env.example with new variables from .env
evnx sync
```

### Reverse sync
```bash
# Pull new variables from .env.example to .env
evnx sync --direction reverse
```

### Preview without changes
```bash
# See what would change before applying
evnx sync --dry-run
```

### CI/CD mode
```bash
# Skip prompts, use placeholders, fail on naming issues
evnx sync --force --dry-run --naming-policy error
```

### Custom placeholder config
```bash
# Use team-specific placeholder patterns
evnx sync --template-config .evnx/placeholders.json
```

---

## Environment variables

| Variable | Values | Default | Description |
|----------|--------|---------|-------------|
| `NO_COLOR` | `1`, `true` | `false` | Disable colored output for terminals |
| `CI` | `1`, `true` | `false` | Auto-enable non-interactive mode |

---

## See also

- [Sync Basics Tutorial](/guides/getting-started/sync-basics) — Learn sync step-by-step
- [Team Collaboration](/guides/use-cases/team-collaboration) — Multi-developer workflows
- [CI/CD Integration](/guides/integrations/ci-cd-sync) — Automation setup
- [Configuration Reference](/guides/reference/sync-configuration) — Advanced options
---